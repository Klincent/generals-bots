name: stop-all-actions

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: evolution2-elitist
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Stop sentinel: cancels evolution2-elitist concurrency group.'
