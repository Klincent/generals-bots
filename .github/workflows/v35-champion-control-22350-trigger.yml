name: Trigger champion control marker
on:
  push:
    branches: [v35-defense-fresh]
    paths:
      - '.github/workflows/v35-champion-control-22350-trigger.yml'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo control workflow already added
